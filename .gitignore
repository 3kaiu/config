# Git Ignore — 3kaiu Loon config
# 2026-09-18 优化审计: 由 68 条精简为 27 条。原文件混入了与本仓库无关的
# 模板残留 (Sublime/Emacs/CVS/Windows/数据库/压缩包/AWS/Sentry/WorkBuddy 等) ——
# 本仓库是 Loon 配置仓, 无这些产物; 保留无用模式只会掩盖"真出现了这类文件"的信号。
# 删掉模式不会影响已跟踪文件 (git 只对未跟踪文件生效), 故为纯收敛。

# ── 依赖 ──
node_modules/
npm-debug.log*
yarn-error.log*

# ── 构建产物 ──
# ⚠️ `dist/` 是承重条目: surgio generate 每次都会创建一个空 dist/ (surgio 自身行为,
#    非 npm 产物), 删掉它会让工作区常驻一个未跟踪目录。勿删, 也勿清理该目录。
dist/
build/
.surgio-cache/
.cache/

# ── 临时/备份文件 ──
*.tmp
*.temp
*.bak
*.backup
*.swp
*~
*.orig
*.old
*.save

# ── 编辑器 ──
.vscode/
.idea/
*.iml
*.code-workspace

# ── 操作系统 ──
.DS_Store
._*
.AppleDouble

# ── 本地环境/密钥 (勿提交任何凭据) ──
.env*
!.env.example
*.pem
*.key
*.secret

# ── 测试覆盖率 ──
coverage/
.nyc_output/

# ── 其他 ──
.eslintcache
scratch/
