#!name=隐私防护
#!desc=全局 DNS 级 REJECT 拦截 Google 广告/分析、国际归因/崩溃分析 SDK、国内统计 SDK 等追踪域名，无需 MitM。
#!version=1.0
#!author=3kaiu
#!homepage=https://github.com/3kaiu/config
#!icon=https://icons.duckduckgo.com/ip3/privacy.ico
#!system = iOS,iPadOS,macOS
#!loon_version = 3.2.4(787)

[Argument]
PRIVACY_ENABLE=switch,"true","false",tag=隐私防护总开关,desc=开启后 REJECT 所有追踪与分析统计域名（纯 DNS 拦截，无需 MitM）

[Rule]
# 开关真实控制的条目 (11): 以下域名仅由本插件拦截 — 关开关即放行。
# 另 64 条追踪域已删 (2026-09-22): 主配置 [Rule] 对它们无条件 REJECT, 本地规则 >
# 插件规则, 本段重复永不可达且开关拨动无任何效果 (MOD-04 同型假粒度, 删后零行为变化)。
# 删掉的名单与成因见 test/cases/plugin-tpl-shadow.test.js 登记; 若主配置删了其中
# 任一条, 该用例即红, 届时把对应条目加回本段。
# ⚠️ 推送保活: 仅拦统计子域, 保留 config.jpush.cn / user.jpush.cn (极光推送) 与 api.getui.com (个推推送)
# 国内统计 / 归因 SDK (主配置无覆盖, 开关有效)
DOMAIN-SUFFIX, umeng.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, umengcloud.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, umeng.co, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, sensors.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, talkingdata.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, growingio.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, mta.qq.com, REJECT, enable={PRIVACY_ENABLE}
# 2026-09-22 纯追踪 SDK 补齐 (DNS-REJECT, 无功能面): 百度统计/诸葛IO/腾讯灯塔-beacon/腾讯Bugly,
# 均为 fire-and-forget 埋点上报, 拦了只丢 dev 看板数据不伤用户功能; gdt 系不碰
# (与 qidian.plugin DIRECT 白名单的层间矛盾未决, 见 B7-1, 不添新变量)
DOMAIN-SUFFIX, hm.baidu.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, zhugeio.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, pingtas.qq.com, REJECT, enable={PRIVACY_ENABLE}
DOMAIN-SUFFIX, bugly.qq.com, REJECT, enable={PRIVACY_ENABLE}

# 纯 DNS 级 REJECT，无需 MitM
[MitM]
# 无 MitM 域名 - 隐私防护全部走 DNS REJECT
