diff --git a/node_modules/surgio/build/config.js b/node_modules/surgio/build/config.js
index d0a2043..b88112a 100644
--- a/node_modules/surgio/build/config.js
+++ b/node_modules/surgio/build/config.js
@@ -120,7 +120,7 @@ const normalizeConfig = (cwd, userConfig) => {
     }
     // istanbul ignore next
     if (!fs_extra_1.default.existsSync(config.providerDir)) {
-        throw new Error(`仓库内缺少 ${config.providerDir} 目录`);
+        // providerDir 并非必需，内联 providers 无需此目录
     }
     if (/http/i.test(config.urlBase)) {
         const urlObject = new url_1.URL(config.urlBase);
diff --git a/node_modules/surgio/build/generator/artifact.js b/node_modules/surgio/build/generator/artifact.js
index aaa63a7..2ad7b65 100644
--- a/node_modules/surgio/build/generator/artifact.js
+++ b/node_modules/surgio/build/generator/artifact.js
@@ -166,14 +166,26 @@ class Artifact extends events_1.EventEmitter {
             artifact: this.artifact,
             providerName,
         });
-        if (!fs_extra_1.default.existsSync(filePath)) {
+        let providerConfig;
+        if (fs_extra_1.default.existsSync(filePath)) {
+            providerConfig = require(filePath);
+        }
+        else if (config.providers) {
+            const match = config.providers.find(function (p) { return p.name === providerName; });
+            if (match) {
+                var rest = Object.assign({}, match);
+                delete rest.name;
+                providerConfig = rest;
+            }
+        }
+        if (!providerConfig) {
             throw new Error(`文件 ${filePath} 不存在`);
         }
         let provider;
         let subscriptionUserInfo;
         let nodeConfigList;
         try {
-            provider = await (0, provider_1.getProvider)(providerName, require(filePath));
+            provider = await (0, provider_1.getProvider)(providerName, providerConfig);
             this.providerMap.set(providerName, provider);
         }
         catch (_err) /* istanbul ignore next */ {
diff --git a/node_modules/surgio/build/validators/surgio-config.js b/node_modules/surgio/build/validators/surgio-config.js
index baa85ea..dc6aa55 100644
--- a/node_modules/surgio/build/validators/surgio-config.js
+++ b/node_modules/surgio/build/validators/surgio-config.js
@@ -101,5 +101,5 @@ exports.SurgioConfigValidator = zod_1.z.object({
             .optional(),
     })
         .optional(),
-});
+}).passthrough();
 //# sourceMappingURL=surgio-config.js.map
\ No newline at end of file
