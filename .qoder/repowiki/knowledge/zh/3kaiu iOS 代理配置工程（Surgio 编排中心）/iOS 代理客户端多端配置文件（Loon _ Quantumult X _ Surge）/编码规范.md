- 每份配置文件顶部以 `# ═══════════ ... (Surgio 生成) ═══════════` 注释块声明版本（v7.8）与核心能力摘要。
- 分流规则按固定顺序组织：银行/云闪付 REJECT → 国内银行 DIRECT → STUN 阻断 → 局域网 → HTTPDNS 拦截 → Apple → 微信 → AI 服务 → 流媒体 → 社交平台 → 开发者平台 → Google 通配 → 分析/广告 REJECT → GEOIP CN → FINAL。
- 策略组命名统一为 Proxy/Apple/Final/Streaming/AI/Developer/Social，并在各文件中保持一致的优先级与默认选择顺序。
- 需要跨客户端同步维护的规则（如银行去广告、STUN 阻断）通过注释 `⚠️ 与 QX.conf [filter_local] / Loon.lcf [Rule] 同步维护 — 修改时务必双端对齐` 显式标注。
- 远程资源（规则集、插件、脚本、MITM 列表）统一通过 `https://ws.wenn.in/main/...` 或 `kelee.one/...` 远端 URL 引用，并附带 `tag=` 与 `enabled=true` 元数据。
- 节点来源通过注释说明由 Surgio 管理，支持通过 `SURGIO_SUBSCRIPTION_URL` 或 Secrets 注入，并强调 url-test/server_regex=.` 容灾机制。