- 每个脚本顶部保留统一格式的注释头，包含应用名称、作者（@ddgksf2013）、微信账号、更新时间、通知频道和版权声明。
- 脚本通过代理运行时全局对象 $request/$response 获取请求上下文，并通过 $done() 返回修改后的响应，形成统一的输入输出契约。
- 错误处理和调试通过 console.log 输出日志，异常情况使用 $notification.post 向用户推送通知，并附带 url 或字段名等上下文信息。
- MANIFEST.json 中每条记录都包含 source_url、sha256、bytes、fetched_at 四个固定字段，由外部自动化流程维护而非手动编辑。