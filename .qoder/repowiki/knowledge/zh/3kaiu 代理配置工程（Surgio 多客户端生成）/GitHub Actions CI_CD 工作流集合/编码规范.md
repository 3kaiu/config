- 每个工作流顶部以中文注释说明触发时机、目的及关联的长期演进编号（如 #13、#1），并在 on.schedule 中使用 UTC cron 表达式错开执行时间避免竞争。
- 所有步骤通过 `echo "..." >> $GITHUB_STEP_SUMMARY` 输出结构化 Markdown 表格报告，便于在 Actions 页面直接查看结果摘要。
- 失败处理统一采用 `actions/github-script@v7` 检查是否存在同 label 的 open Issue，存在则跳过创建以避免重复告警。
- 需要写权限的工作流显式声明 `permissions: contents: write`，仅读操作声明 `contents: read`，遵循最小权限原则。
- 外部资源抓取统一使用 `curl -fsSL -m <timeout>` 格式，超时时间根据资源大小设定（30s/60s/90s），失败时保留旧版本或跳过而非中断流水线。